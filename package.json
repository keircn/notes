{
  "name": "notes",
  "type": "module",
  "private": true,
  "scripts": {
    "format": "prettier --write notes",
    "lint:md": "markdownlint notes/**/*.md",
    "prepare": "bun run format && bun run lint:md"
  },
  "dependencies": {
    "markdownlint-cli": "^0.45.0",
    "prettier": "^3.6.2"
  }
}
